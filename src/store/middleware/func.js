//#region
// const func =
//   ({ dispatch, getState }) =>
//   (next) =>
//   (action) => {
//     if (typeof action === "function") action(dispatch, getState);
//     else next(action);
//   };

// export default func;

//This middleware is used to let functions to be dispatched . Like this ====> store.dispatch(() => {})
//No need to create this when using redux toolkit. You can import it from redux toolkit

//#endregion
